<% if @user.errors.any? %>
  <div>
    <h3><%= pluralize(@user.errors.count, "error") %> detected:</h3>
    <ul>
    <% @user.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  <div>
<% end %>
